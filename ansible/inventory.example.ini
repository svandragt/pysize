[pysize]
myserver ansible_host=YOUR_SERVER_IP ansible_user=YOUR_SSH_USER

# Example:
# myserver ansible_host=203.0.113.10 ansible_user=root
# If you log in as a non-root sudo user, the playbook already uses `become: true`.
